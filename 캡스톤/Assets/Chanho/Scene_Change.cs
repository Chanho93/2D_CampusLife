using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class Scene_Change : MonoBehaviour
{

    public GameObject Player;
    public GameObject P_Camera;

   

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void OnTriggerEnter2D(Collider2D other)
    {
        /*
        if(other.CompareTag("School"))
        {
            Vector2 originPoint = new Vector2();
            originPoint.x = 2.1f;
            originPoint.y = 27.75f;
            Player.transform.position = originPoint;

            originPoint.x = 2.1f;
            originPoint.y = 27.75f;
            P_Camera.transform.position = originPoint;
           
        }
        else if(other.CompareTag("Home"))
        {
            Vector2 originPoint = new Vector2();
            originPoint.x = 34.22132f;
            originPoint.y = 15.87868f;
            Player.transform.position = originPoint;

            originPoint.x = 34.22132f;
            originPoint.y = 15.87868f;
            P_Camera.transform.position = originPoint;
        }
        else if (other.CompareTag("PC"))
        {
            Vector2 originPoint = new Vector2();
            originPoint.x = -38.48285f;
            originPoint.y = 13.45139f;
            Player.transform.position = originPoint;

            originPoint.x = -38.48285f;
            originPoint.y = 13.45139f;
            P_Camera.transform.position = originPoint;
        }
        else if (other.CompareTag("Book"))
        {
            Vector2 originPoint = new Vector2();
            originPoint.x = -4.425125f;
            originPoint.y = -35.47488f;
            Player.transform.position = originPoint;

            originPoint.x = -4.425125f;
            originPoint.y = -35.47488f;
            P_Camera.transform.position = originPoint;
        }
        else if (other.CompareTag("Cafe"))
        {
            Vector2 originPoint = new Vector2();
            originPoint.x = -27.83934f;
            originPoint.y = -24.56066f;
            Player.transform.position = originPoint;

            originPoint.x = -27.83934f;
            originPoint.y = -24.56066f;
            Player.transform.position = originPoint;
            P_Camera.transform.position = originPoint;
        }
        else if (other.CompareTag("Beer"))
        {
            Vector2 originPoint = new Vector2();
            originPoint.x = 29.61041f;
            originPoint.y = -21.01041f;
            Player.transform.position = originPoint;

            originPoint.x = 29.61041f;
            originPoint.y = -21.01041f;
            Player.transform.position = originPoint;
            P_Camera.transform.position = originPoint;
        }
        else if (other.CompareTag("Campus"))
        {
            Vector2 originPoint = new Vector2();
            originPoint.x = 2.6f;
            originPoint.y = 0f;
            Player.transform.position = originPoint;

            originPoint.x = 2.6f;
            originPoint.y = 0f;
            Player.transform.position = originPoint;
            P_Camera.transform.position = originPoint;
        }

    */
    }

    

   


}
